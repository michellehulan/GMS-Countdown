<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Days Until Dec 2</title>
<style>
  body {
    font-family: Arial, sans-serif;
    font-size: 40px;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
  }
</style>
</head>
<body>
<div id="days"></div>

<script>
  function update() {
    const today = new Date();
    const target = new Date(today.getFullYear(), 11, 2); // December = month 11

    // If December 2 has passed this year, count toward next year's
    if (today > target) {
      target.setFullYear(target.getFullYear() + 1);
    }

    const diff = target - today;
    const days = Math.ceil(diff / (1000 * 60 * 60 * 24));

    document.getElementById('days').textContent = days + " days";
  }

  update();
  setInterval(update, 1000 * 60 * 60); // update every hour
</script>
</body>
</html>
